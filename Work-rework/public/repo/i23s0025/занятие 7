<!DOCTYPE html>
    <html lang="en">
    <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Таблица</title>
    </head>
    <body>
     
     
        <style>
        table{
            width: 50px;
            height: 50px;
            border: 1px solid black;
            }
     
            td, tr {
                border: 1px solid black; 
            }
            </style>
     
     
            <h1>Шахматная доска</h1>
            <p><input id="num1" type="number" placeholder="напишите количество строк"></p>
            <p><input id="num2" type="number" placeholder="напишите количество столбцов"></p>
            <button onclick="work()">Ok</button>
            <p id="outp"></p>
     
     
            <script>
                function work() {
                    
                    var table = document.querySelector('table');
                    if (table) {
                        table.remove();
                    }
     
                    rowll = parseFloat(document.getElementById('num1').value);
                    coll = parseFloat(document.getElementById('num2').value);
                    table = document.createElement('table');
                    outp = document.getElementById('outp');
     
     
                    if (isNaN(rowll) || isNaN(coll)) {
                        outp.innerText = 'напиши число'
                    } else {
                        for (let i = 0; i < row; i++) {
                            tr = document.createElement('tr');
                            for (let j = 0; j < coll; j++) {
                                td = document.createElement('td');
                                td.textContent = i * coll + j + 1;
                                if ((i + j) % 2 === 0) {
                                    td.style.backgroundColor = 'pink';
                                    td.style.color = 'black';
                                }
     
                                tr.appendChild(td);
                            }
                            table.appendChild(tr);
                        }
                        document.body.appendChild(table);
                    }
                }
            </script>
        </body>
    </html>
